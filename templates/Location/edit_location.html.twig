{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier le lieu</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Modifier le lieu</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_location_list') }}">Retour à la liste des lieux</a>
{% endblock %}
