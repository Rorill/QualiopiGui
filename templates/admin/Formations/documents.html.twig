{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto my-8 p-6 bg-white shadow-md rounded-lg">
        <h1 class="text-3xl font-bold mb-4 text-gray-800">Documents de {{ formateur.firstName }} {{ formateur.lastName }} pour la formation : {{ formation.name }}</h1>

        <table class="min-w-full divide-y divide-gray-200 bg-white">
            <thead class="bg-gray-50">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nom du Document</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date d'Upload</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Action</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            {% for document in documents %}
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ document.title }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{ document.uploadedAT|date('d/m/Y') }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                        <a href="{{ path('app_document_download', { id: document.id }) }}" class="text-blue-600 hover:text-blue-800">Télécharger</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3" class="px-6 py-4 text-center text-sm text-gray-500">Aucun document trouvé.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_formations') }}" class="mt-4 inline-block text-blue-600 hover:text-blue-800">Retour à la liste des formations</a>
    </div>
{% endblock %}

